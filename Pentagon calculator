"""
Program a system that takes the length of one side of an Octagon and outputs the resultant area of the pentagon
"""
#User imput
from math import sqrt

a= float(input("Enter lenght of side a[cm]: "))

#Operation
Area = 1/4*sqrt(5*(5+2*sqrt(5))*a*a)

#Result of operation
print(f"Area of pentagon: {Area}cm^2")
